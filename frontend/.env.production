# Frontend environment variables — production build
# This file IS committed (no secrets — only public URLs).

# Backend API base URL (points to Render deployment)
VITE_API_URL=https://seven-day-interview.onrender.com/api/v1
